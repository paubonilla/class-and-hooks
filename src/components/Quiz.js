import React, { useState } from 'react'

function Quiz() {
    const userAnswer = null
    const currentIndex = 0
    const options = []
    const quizEnd = false
    const score = 1
    const disabled = true
    return (
        <div>

        </div>
    )
}

export default Quiz
